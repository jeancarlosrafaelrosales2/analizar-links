[build]
dockerfilePath = "Dockerfile"
dockerBuildTarget = "api"

[deploy]
startCommand = "analizar-links"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
